model User {
    id        Int      @id @default(autoincrement())
    email     String
    password  String
    posts     Post[]
    createdAt DateTime @default(now())
    updatedAt DateTime @default(now())
}
